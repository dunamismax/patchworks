# Fixtures

`create_fixtures.sql` contains named fixture blocks used by integration tests. Each block starts with `-- @fixture <name>` and is executed into a temporary SQLite database during the test run.
